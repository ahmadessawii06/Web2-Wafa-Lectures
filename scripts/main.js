import { renderLectureCards } from './ui.js';
import { initBackground } from './background.js';

document.addEventListener("DOMContentLoaded", () => {
    renderLectureCards();
    initBackground();
});